This is a `no_std` port of [egui](https://github.com/emilk/egui/). Anything that references `std` are replaced with `core`, `alloc`, `hashbrown` and `libm`, or simply removed if it doesn't fit `no_std`.
